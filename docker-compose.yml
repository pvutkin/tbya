version: '3.8'

services:
  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      POSTGRES_DB: tbya_portal
      POSTGRES_USER: tbya_user
      POSTGRES_PASSWORD: tbya_password
    ports:
      - "5432:5432"

volumes:
  postgres_data: